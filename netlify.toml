[build]
  publish = ".next"
  command = "npm run build"

# Forward requests to /legacy if the browser sends a cookie 
# named `is_legacy` or `my_other_cookie`
[[redirects]]
from = "/*"
to = "https://www-silversea.uat.bbhosted.com/:splat"
status = 200